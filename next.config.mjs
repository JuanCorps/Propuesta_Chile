/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/CHErasmus', // Replace with your repository name
  // assetPrefix: '/CHErasmus',
};

export default nextConfig;
